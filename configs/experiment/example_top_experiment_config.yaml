configs:
  data_configs:
    - example_tofu_1
    - example_tofu_2
  model_config:
    - gpt2
# TODO: add other configs that define a group of experiments end to end

use_bask: true
bask:
  walltime: '3-0:0:0'
  gpu_number: 1
  node_number: 1
  cpu_per_gpu: 36
